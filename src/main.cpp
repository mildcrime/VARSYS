// ============================================================================
//  main.cpp — Точка входа прошивки VARSYS
//
//  Вся логика инкапсулирована в ядре (Core). setup/loop лишь делегируют.
// ============================================================================
#include <Arduino.h>
#include "core/Core.h"

void setup() {
    Core::instance().begin();
}

void loop() {
    Core::instance().loop();
}
