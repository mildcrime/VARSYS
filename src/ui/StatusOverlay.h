// ============================================================================
//  StatusOverlay.h — Глобальный статус-бар (время + батарея)
//
//  Живёт на верхнем слое LVGL (lv_layer_top), поэтому виден поверх всех
//  экранов и не пересоздаётся при их смене. Обновляется по таймеру раз в
//  секунду из Clock и PowerModule. Заменяет прежние заглушки «9:41».
// ============================================================================
#pragma once
#include <lvgl.h>

class StatusOverlay {
public:
    void create();          // создать на lv_layer_top()
    void update();          // обновить текст (время + заряд)
    void setVisible(bool v);

private:
    lv_obj_t* _label = nullptr;
};
