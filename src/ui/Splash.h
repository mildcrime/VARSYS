// ============================================================================
//  Splash.h — Анимированная заставка-бренд VARSYS (оверлей верхнего слоя)
//
//  Полноэкранный чёрный оверлей на lv_layer_top — ГАРАНТИРОВАННО поверх всех
//  экранов и статус-бара. Слово VARSYS проявляется «из ниоткуда», затем
//  оверлей плавно гаснет, открывая интерфейс. Используется при включении и
//  при выходе из сна.
// ============================================================================
#pragma once
#include <lvgl.h>

namespace Splash {

void create();                       // один раз в UIManager::init()
void play(uint32_t ms = 2000);       // показать + анимировать + авто-скрытие

} // namespace Splash
