// ============================================================================
//  Clock.h — Источник времени VARSYS
//
//  Сейчас опирается на системные часы ESP32 (settimeofday). Реального RTC на
//  плате нет, поэтому время стартует с дефолта и будет уточнено по NTP, когда
//  появится Wi-Fi (этап 4). Этого достаточно, чтобы статус-бар показывал
//  идущее время вместо заглушки.
// ============================================================================
#pragma once
#include <Arduino.h>
#include <stddef.h>

namespace Clock {

// Инициализация системных часов дефолтным временем.
void begin();

// Форматирует текущее время как "HH:MM" в buf.
void timeString(char* buf, size_t n);

} // namespace Clock
